# Launch two foot terminals with the same app_id, then retarget specifically
# the first one by its step id -- something --existing has no way to express,
# since --existing --app-id foot would be ambiguous between the two. Run
# with:
#   sway-launch --layout examples/layouts/retarget-by-id.toml

[[step]]
id = "first"
command = "foot"
app_id = "foot"

[[step]]
command = "foot"
app_id = "foot"
split = "h"

[[step]]
target_id = "first"
floating = true
width = "800px"
height = "600px"
